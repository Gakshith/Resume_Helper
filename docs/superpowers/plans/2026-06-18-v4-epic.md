# Implementation Plan — v4 Epic

Date: 2026-06-18
Spec: `../specs/2026-06-18-v4-epic-design.md`
Branch: `feature/v4-epic`

## Task order (verify after each)

1. **TDD analysis** — write failing tests for `generate_cover_letter` and `rank_jobs`,
   then implement. Keep all prior tests green.
2. **app.py routes** — `/landing` (public, `/`→`/landing`), `/cover-letter` GET/POST,
   `/compare` GET + `/compare-jobs` POST (JSON). Reuse `_record_for`.
3. **CSS** — animated mesh-gradient background (replace static radial `body::before`),
   `prefers-reduced-motion` guard; light/dark via vars.
4. **Templates** — `landing.html`, `cover_letter.html`, `compare.html`; add nav +
   "Download report" + "Cover letter" + "Compare jobs" buttons on the dashboard.
5. **PDF report** — dashboard build-and-export via html2pdf from a print DOM.
6. **Verify live** — landing, cover letter, compare ranking, PDF download, both themes,
   new background; no console errors. Screenshots → `Artifacts/`.
7. **Swarm code-review** — dispatch reviewer agent(s); fix findings.
8. **Finish** — commit, merge to main; update `Plans/ROADMAP.md` + Ruflo memory.

## Notes / risks

- Shared files (app.py, app.css, base.html) edited sequentially by me to avoid merge
  conflicts; swarm used for the end review (parallel-safe).
- Cover-letter name extraction is heuristic (first non-numeric, non-heading line);
  acceptable, documented.
